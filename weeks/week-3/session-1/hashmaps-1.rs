use std::collections::HashMap;
// use std::collections::*; // https://doc.rust-lang.org/std/collections/index.html

fn main() {
    let mut hashmap_instance = HashMap::new();
    hashmap_instance.insert("key", "value");
    hashmap_instance.insert("key2", "value2");

    // JAVA
    // Map<String, String> mapStr = new HashMap<String, String>();
    // mapStr.put("key","value");


    // python
    // mapStr = dict({'key':'value'})
}